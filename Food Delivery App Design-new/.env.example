# Google Maps API Key (optional — SVG dark map fallback works without it)
# Get one at https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_MAPS_KEY=

# App Environment
VITE_APP_ENV=production
